/**
 * RegisterController
 *
 * @description :: Server-side actions for handling incoming requests.
 * @help        :: See https://sailsjs.com/docs/concepts/actions
 */

module.exports = {
  

  /**
   * `RegisterController.username()`
   */
  username: async function (req, res) {
    return res.json({
      todo: 'username() is not implemented yet!'
    });
  },

  /**
   * `RegisterController.email()`
   */
  email: async function (req, res) {
    return res.json({
      todo: 'email() is not implemented yet!'
    });
  },

  /**
   * `RegisterController.passwd()`
   */
  passwd: async function (req, res) {
    return res.json({
      todo: 'passwd() is not implemented yet!'
    });
  }

};

